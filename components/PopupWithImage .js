class PopupWithImage extends Popup {


  openPopup(elem) {//открывает попап 
      // elem.addEventListener('click', mouseClickOverlay);
      // document.addEventListener('keydown', closePopupButtonEsc);
      // elem.classList.add('popup_open');
  }
  
}